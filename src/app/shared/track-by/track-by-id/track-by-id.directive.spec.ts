import { TrackByIdDirective } from './track-by-id.directive';

describe('Directive: TrackById', () => {
  it('should create an instance', () => {
    const directive = new TrackByIdDirective();

    expect(directive).toBeTruthy();
  });
});
